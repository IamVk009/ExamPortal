spring:
  application:
    name: QuestionService

  datasource:
    url: jdbc:mysql://localhost:3306/exam_db
    username: root
    password: root

  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update

server:
  port: 8082